server:
  port: 8080

spring:
  application:
    name: spring-cloud-mastery

  datasource:
    url: jdbc:postgresql://localhost:5432/cloud_db
    username: ${DB_USER:postgres}
    password: ${DB_PASS:root7769}
    driver-class-name: org.postgresql.Driver

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZona: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${server.port}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

jwt:
  secret: gTWu/dFHt4PfVHbaVU3lEPUMot8Hu/FoW+zFsmzrGn9sqYpDTS8yRx7JOE3nSsGDe+DYLjvHhQwYYOuhOJ17fw==


